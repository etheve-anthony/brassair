<form method="post" action="{{ path('app_product_offer_delete', {'id': product_offer.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette offre?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_offer.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
