{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ product.title }}</td>
            </tr>
            <tr>
                <th>Caracteristique_1</th>
                <td>{{ product.caracteristique1 }}</td>
            </tr>
            <tr>
                <th>Caracteristique_2</th>
                <td>{{ product.caracteristique2 }}</td>
            </tr>
            <tr>
                <th>Caracteristique_3</th>
                <td>{{ product.caracteristique3 }}</td>
            </tr>
            <tr>
                <th>Caracteristique_4</th>
                <td>{{ product.caracteristique4 }}</td>
            </tr>
            <tr>
                <th>PdfName</th>
                <td>{{ product.pdfName }}</td>
            </tr>
            <tr>
                <th>PdfSize</th>
                <td>{{ product.pdfSize }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ product.updatedAt ? product.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_index') }}">back to list</a>

    <a href="{{ path('app_product_edit', {'id': product.id}) }}">edit</a>

    {{ include('product/_delete_form.html.twig') }}
{% endblock %}
