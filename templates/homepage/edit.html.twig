{% extends 'base.html.twig' %}

{% block title %}Editer page d'accueil
{% endblock %}

{% block body %}
	<div class="d-none d-lg-block">
		{% include 'navbar_lg_admin.html.twig' %}
	</div>

	<div class="d-block d-lg-none">
		{% include 'navbar_md_admin.html.twig' %}
	</div>
	<div class="mx-5 mb-5">
		<h1 class="mt-3">Editer la page d'accueil</h1>
		{{ include('homepage/_form.html.twig', {'button_label': 'Modifier'}) }}
		<a href="{{ path('app_homepage_index') }}">Revenir à la liste</a>
	</div>
{% endblock %}
