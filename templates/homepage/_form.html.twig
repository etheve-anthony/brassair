{{ form_start(form) }}

<div class="row">
  <h3>Section 1 (accueil)</h3>
  <div class="col-lg-4">{{ form_row(form.hero1) }}</div>
  <div class="col-lg-4">{{ form_row(form.hero2) }}</div>
  <div class="col-lg-4">{{ form_row(form.mainImage) }}</div>
</div>
<div class="row">
  <div class="col-lg-4">{{ form_row(form.hero1Part2) }}</div>
</div>
<hr />
<div class="row">
  <h3>Solutions de ventilation</h3>
  <div class="col-lg-4">{{ form_row(form.titleSection2Small) }}</div>
  <div class="col-lg-4">{{ form_row(form.titleSection2Big) }}</div>
</div>

<details><summary>Anciens pictogrammes (non affichés sur l’accueil)</summary><div class="row">
  <div class="col-lg-3">{{ form_row(form.promise1) }}</div>
  <div class="col-lg-3">{{ form_row(form.promise2) }}</div>
  <div class="col-lg-3">{{ form_row(form.promise3) }}</div>
  <div class="col-lg-3">{{ form_row(form.promise4) }}</div>
</div>

<div class="row">
  <div class="col-lg-3">{{ form_row(form.promise1Image) }}</div>
  <div class="col-lg-3">{{ form_row(form.promise2Image) }}</div>
  <div class="col-lg-3">{{ form_row(form.promise3Image) }}</div>
  <div class="col-lg-3">{{ form_row(form.promise4Image) }}</div>
</div>
</details><hr />
<div class="row">
  <h3>Réalisations</h3>
  <div class="col-lg-4">{{ form_row(form.titleSection3) }}</div>
  <div class="col-lg-4"></div>
</div>
<hr />
<div class="row">
  <h3>Présentation BRASS’AIR</h3>
  <div class="col-lg-4">{{ form_row(form.aboutText1) }}</div>
  <div class="col-lg-4">{{ form_row(form.aboutText2) }}</div>
</div>

<button class="btn btn-success mt-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
<div class="my-4">
  <small>Les boutons Actualités BRASS’AIR et Demander une étude ont des libellés fixes.</small><br />
  <small>Les photos des secteurs et des réalisations sont définies dans le template. L’image principale reste administrable.</small>
  <br />
  <small>Les vidéos des réalisations restent hébergées dans public/videos.</small><br />
  <small>****Les informations de contact peuvent être changés dans l'administration dédiée à cet effet.</small>
</div>
