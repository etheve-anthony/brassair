{% extends 'base.html.twig' %}

{% block title %}Index images
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-3">Index des fichiers images</h1>

		<table class="table">
			<thead>
				<tr>
					<th>Id</th>
					<th>Nom</th>
					<th>Taille</th>
					<th>Modifié le</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				{% for image_file in image_files %}
					<tr>
						<td>{{ image_file.id }}</td>
						<td>{{ image_file.imageName }}</td>
						<td>{% set fileSizeInBytes = image_file.imageSize %}{% set fileSizeInMegabytes = fileSizeInBytes / (1024 * 1024) %}
							{{ fileSizeInMegabytes | number_format(2) }}
							Mo</td>
						<td>{{ image_file.updatedAt ? image_file.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
						<td>
							<a href="{{ path('app_image_file_show', {'id': image_file.id}) }}">voir</a>
							<a href="{{ path('app_image_file_edit', {'id': image_file.id}) }}">éditer</a>
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="5">Pas d'images trouvées</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		<a href="{{ path('app_image_file_new') }}">Ajouter une nouvelle image</a>
	</div>
{% endblock %}
