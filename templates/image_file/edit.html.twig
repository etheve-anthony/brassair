{% extends 'base.html.twig' %}

{% block title %}Editer image
{% endblock %}

{% block body %}
	<div class="d-none d-lg-block">
		{% include 'navbar_lg_admin.html.twig' %}
	</div>

	<div class="d-block d-lg-none">
		{% include 'navbar_md_admin.html.twig' %}
	</div>
	<div class="mx-5 mt-3">
		<h1 class="mt-3">Editer fichier image</h1>

		{{ include('image_file/_form.html.twig', {'button_label': 'Modifier'}) }}
		<a href="{{ 'https://bluetec.re/images/' ~ image_id }}" download>Télécharger</a>
		<br>
		<hr>
		<a href="{{ path('app_image_file_index') }}">Revenir à la liste</a>

		{{ include('image_file/_delete_form.html.twig') }}
	</div>
{% endblock %}
