{% extends "base.html.twig" %}

{% block title %}Suppression d'utilisateurs
{% endblock %}

{% block body %}

	<section class="container my-3">
		<div class="row">
			<div class="col">
				<h2 class="text-center">Utilisateurs</h2>
				<p class="text-center lead text-danger">Supprimer un utilisateur</p>


				{{ form_start(user) }}


				<label>Prénom</label>
				{{ form_widget(user.first_name)}}

				<label class="mt-3">Nom</label>
				{{ form_widget(user.last_name) }}


				<button type="submit" class="btn btn-danger my-3">Supprimer</button>
				{{ form_end(user) }}
			</div>
		</div>
	</section>

{% endblock %}
