{% extends 'base_admin.html.twig' %}

{% block title %}Modifier produit
{% endblock %}

{% block body %}
	<div class="d-none d-lg-block">
		{% include 'navbar_lg_admin.html.twig' %}
	</div>

	<div class="d-block d-lg-none">
		{% include 'navbar_md_admin.html.twig' %}
	</div>
	<div class="mx-5 mb-5">
		<h1 class="mt-3">Editer produit</h1>

		{{ include('kitchen/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a class="mb-3" href="{{ path('app_kitchen_index') }}">Revenir à la liste</a>
		<hr>

		{{ include('kitchen/_delete_form.html.twig') }}
	</div>
{% endblock %}
