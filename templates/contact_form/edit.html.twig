{% extends 'base_admin.html.twig' %}

{% block title %}Edit ContactForm
{% endblock %}

{% block body %}

	<div class="d-none d-lg-block">
		{% include 'navbar_lg_admin.html.twig' %}
	</div>

	<div class="d-block d-lg-none">
		{% include 'navbar_md_admin.html.twig' %}
	</div>
	<div class="mx-5 mb-5">
		<h1 class="mt-3">Editer le formulaire de contact</h1>

		{{ include('contact_form/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a href="{{ path('app_contact_form_index') }}">Revenir à la liste</a>

		{{ include('contact_form/_delete_form.html.twig') }}

	</div>

{% endblock %}
